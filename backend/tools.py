"""
Tools module (currently empty).

All Pokémon tools are now provided by the remote MCP server (mcpokedex.com/mcp).
The local @tool decorator was removed to simplify the architecture and rely
entirely on the MCP server's tools (get_pokedex_entry, analyze_pokemon_stats,
analyze_team, get_type_effectiveness, search_pokemon, etc.).
"""
